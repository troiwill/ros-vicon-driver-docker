version: '3'
services:
  vicon-driver:
    build:
      context: ./
      args:
        # Change to Vicon server IP address. The current value WILL NOT WORK.
        vicon_server_ip: 127.0.0.1
    container_name: vicon-driver-c
    tty: true
    image: vicon-driver
    network_mode: "host"
    environment:
      # Substitute "HOST_IP" in both environment variable for the name of your computer.
      ROS_HOSTNAME: HOST_IP
      ROS_MASTER_URI: http://HOST_IP:11311
      ROS_IP: HOST_IP
